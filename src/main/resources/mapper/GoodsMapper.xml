<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tstar.seckill.mapper.GoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tstar.seckill.pojo.Goods">
        <id column="id" property="id" />
        <result column="good_name" property="goodName" />
        <result column="good_title" property="goodTitle" />
        <result column="goods_img" property="goodsImg" />
        <result column="goods_detail" property="goodsDetail" />
        <result column="good_price" property="goodPrice" />
        <result column="good_stock" property="goodStock" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, good_name, good_title, goods_img, goods_detail, good_price, good_stock
    </sql>
    <select id="findGoodsVo" resultType="com.tstar.seckill.vo.GoodsVo">
        SELECT
            g.id,
            g.good_name,
            g.good_title,
            g.goods_img,
            g.goods_detail,
            g.good_price,
            g.good_stock,
            sg.seckill_price,
            sg.stock_count,
            sg.start_date,
            sg.end_date
        FROM
            t_goods g
            LEFT JOIN t_seckill_goods AS sg ON g.id = sg.goods_id
    </select>
    <select id="findGoodsVoByGoodsId" resultType="com.tstar.seckill.vo.GoodsVo">
        SELECT
            g.id,
            g.good_name,
            g.good_title,
            g.goods_img,
            g.goods_detail,
            g.good_price,
            g.good_stock,
            sg.seckill_price,
            sg.stock_count,
            sg.start_date,
            sg.end_date
        FROM
            t_goods g
                LEFT JOIN t_seckill_goods AS sg ON g.id = sg.goods_id
        WHERE g.id = #{goodsId}
    </select>


</mapper>
